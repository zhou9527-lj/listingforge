---
title: 发布方案
status: 已确定
tags: [商品图匠, 发布, 签名]
---

# 发布方案

## 目标产物

- Windows 10/11 x64：NSIS 安装包，视需要补充 MSI。
- macOS 12+：Intel DMG、Apple Silicon DMG；可额外提供 universal 构建。
- 源码与校验和：GitHub Releases。

## Windows 签名

项目公开发布并满足持续维护等条件后申请 SignPath Foundation 免费开源签名。该服务需要外部审批，不能保证通过；审批前发布未签名测试包并清楚说明 SmartScreen 风险。

## macOS 签名

用户不购买 Apple Developer 计划，因此不做 Developer ID 公证。使用 ad-hoc 签名，发布页提供右键“打开”或系统设置允许打开的说明，并明确 Gatekeeper 体验限制。

## GitHub

仓库为 `https://github.com/zhou9527-lj/listingforge`，MIT 许可证。接入时仅使用浏览器 OAuth、SSH 或最小权限 Token，绝不接收用户密码。

### 手动桌面构建

- GitHub 页面可手动运行 `Desktop build`。
- 无 GitHub CLI/API 授权时，可通过 SSH 推送 `build-*` 标签触发同一工作流，例如 `build-ca76efb`。普通分支推送不会触发桌面打包。
- Windows 产物为 `listingforge-windows-x64` NSIS 安装包；macOS 产物为 Apple Silicon/Intel app 与 DMG。

### 最新已验证构建（2026-08-09）

- GitHub Actions run：`31297679913`，触发标签 `build-ca76efb`，构建提交 `80e8cf6`；该提交包含应用代码 `ca76efb` 的“删除内部顶栏”更新。
- Windows x64 NSIS：构建成功，artifact `listingforge-windows-x64`，artifact ID `9033543418`，压缩包大小 `6,569,006` bytes，SHA-256 digest `02563a37f50369891242878af2972ddd7acd3cfb9aca8310a6ad81c9e2baf19e`。
- macOS Apple Silicon：构建成功，artifact `listingforge-macos-apple-silicon`，artifact ID `9033505750`，压缩包大小 `17,716,545` bytes，SHA-256 digest `a3de4ddccfd5822184d0d470ace85c2cfb3f5fef187d4470df94edd87e643d83`。
- 下载页：`https://github.com/zhou9527-lj/listingforge/actions/runs/31297679913`。在页面底部 Artifacts 区点击对应产物；GitHub Actions artifact 下载通常需要登录 GitHub。
- macOS Intel 的 `macos-13` job 仍排队且没有产物；该 runner 镜像已退役，不能把它记录为构建成功。

### 0.1.1 项目创建修复包

- 修复 Tauri 命令以字符串 reject 时真实错误被前端吞掉、只显示“创建项目失败”的问题；项目读取、创建、重命名、删除及目录选择均显示后端原始可操作错误。
- Rust 创建目录与写入 `project.json` 的错误现在包含目标路径和系统错误；新增 Windows 中文项目名真实创建测试，验证 `assets/results/canvas/exports/logs` 与项目清单均可创建。
- `desktop-build.yml` 在 Windows NSIS 打包前运行该真实创建测试，失败时不会上传安装包。
- 应用版本升级为 `0.1.1`。GitHub Actions run `31299590619` 的 Windows 中文项目创建测试、NSIS 打包与 artifact 上传均成功；Windows artifact `listingforge-windows-x64`（ID `9034166324`，6,596,961 bytes，digest `sha256:868134d8dd0c164bb0a99a6ef6cd8020f1594b194f56179ea42ea8cadbde55ec`）。下载页：`https://github.com/zhou9527-lj/listingforge/actions/runs/31299590619`。

### 0.1.2 交互与 Agent 完整版

- 三端构建矩阵：`windows-latest` 生成 NSIS，`macos-14` 生成 Apple Silicon app/dmg，`macos-15-intel` 生成 Intel app/dmg。
- `macos-13` 已退役，不能继续使用；Intel 构建按 GitHub 当前标准 runner 标签迁移到 `macos-15-intel`。
- 发布前必须通过 `npm run check`、`npm run test:click`、Windows 中文项目创建 Rust 测试，以及三端 Tauri 构建。安装包 artifact 和 SHA-256 在 Actions 完成后写回本文。
- 同一 run 的 macOS Apple Silicon artifact `listingforge-macos-apple-silicon` 也成功（ID `9034101781`，17,721,529 bytes，digest `sha256:dc1719795e01dcaa20ea65824d1e4152c5d5ede8efe8bf8256f5ffc133780f56`）；macOS Intel 仍因 `macos-13` runner 退役而排队。

## 发布检查

版本号、变更日志、许可证、第三方声明、模型许可证、SBOM、测试报告、SHA-256、安装/卸载、离线启动、密钥迁移和工程兼容性均通过后才发布。

## 自动更新

不加入自动更新功能。用户从 GitHub Releases 手动下载新版本，应用只在“关于”页显示当前版本，不主动检查更新。
