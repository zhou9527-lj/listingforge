---
title: 发布方案
status: 已确定
tags: [商品图匠, 发布, 签名]
---

# 发布方案

## 目标产物

- Windows 10/11 x64：NSIS 安装包，视需要补充 MSI。
- macOS 12+：Intel DMG、Apple Silicon DMG；可额外提供 universal 构建。
- 源码与校验和：GitHub Releases。

## Windows 签名

项目公开发布并满足持续维护等条件后申请 SignPath Foundation 免费开源签名。该服务需要外部审批，不能保证通过；审批前发布未签名测试包并清楚说明 SmartScreen 风险。

## macOS 签名

用户不购买 Apple Developer 计划，因此不做 Developer ID 公证。使用 ad-hoc 签名，发布页提供右键“打开”或系统设置允许打开的说明，并明确 Gatekeeper 体验限制。

## GitHub

仓库为 `https://github.com/zhou9527-lj/listingforge`，MIT 许可证。接入时仅使用浏览器 OAuth、SSH 或最小权限 Token，绝不接收用户密码。

### 手动桌面构建

- GitHub 页面可手动运行 `Desktop build`。
- 无 GitHub CLI/API 授权时，可通过 SSH 推送 `build-*` 标签触发同一工作流，例如 `build-ca76efb`。普通分支推送不会触发桌面打包。
- Windows 产物为 `listingforge-windows-x64` NSIS 安装包；macOS 产物为 Apple Silicon/Intel app 与 DMG。

### 最新已验证构建（2026-08-09）

- GitHub Actions run：`31297679913`，触发标签 `build-ca76efb`，构建提交 `80e8cf6`；该提交包含应用代码 `ca76efb` 的“删除内部顶栏”更新。
- Windows x64 NSIS：构建成功，artifact `listingforge-windows-x64`，artifact ID `9033543418`，压缩包大小 `6,569,006` bytes，SHA-256 digest `02563a37f50369891242878af2972ddd7acd3cfb9aca8310a6ad81c9e2baf19e`。
- macOS Apple Silicon：构建成功，artifact `listingforge-macos-apple-silicon`，artifact ID `9033505750`，压缩包大小 `17,716,545` bytes，SHA-256 digest `a3de4ddccfd5822184d0d470ace85c2cfb3f5fef187d4470df94edd87e643d83`。
- 下载页：`https://github.com/zhou9527-lj/listingforge/actions/runs/31297679913`。在页面底部 Artifacts 区点击对应产物；GitHub Actions artifact 下载通常需要登录 GitHub。
- macOS Intel 的 `macos-13` job 仍排队且没有产物；该 runner 镜像已退役，不能把它记录为构建成功。

### 0.1.1 项目创建修复包

- 修复 Tauri 命令以字符串 reject 时真实错误被前端吞掉、只显示“创建项目失败”的问题；项目读取、创建、重命名、删除及目录选择均显示后端原始可操作错误。
- Rust 创建目录与写入 `project.json` 的错误现在包含目标路径和系统错误；新增 Windows 中文项目名真实创建测试，验证 `assets/results/canvas/exports/logs` 与项目清单均可创建。
- `desktop-build.yml` 在 Windows NSIS 打包前运行该真实创建测试，失败时不会上传安装包。
- 应用版本升级为 `0.1.1`。GitHub Actions run `31299590619` 的 Windows 中文项目创建测试、NSIS 打包与 artifact 上传均成功；Windows artifact `listingforge-windows-x64`（ID `9034166324`，6,596,961 bytes，digest `sha256:868134d8dd0c164bb0a99a6ef6cd8020f1594b194f56179ea42ea8cadbde55ec`）。下载页：`https://github.com/zhou9527-lj/listingforge/actions/runs/31299590619`。

### 0.1.2 交互与 Agent 完整版

- 三端构建矩阵：`windows-latest` 生成 NSIS，`macos-14` 生成 Apple Silicon app/dmg，`macos-15-intel` 生成 Intel app/dmg。
- `macos-13` 已退役，不能继续使用；Intel 构建按 GitHub 当前标准 runner 标签迁移到 `macos-15-intel`。
- 功能提交 `233e5a9`；构建配置修复提交 `cf0dbc7`，启用 DeepSeek 流式取消所需的 Tokio `macros` 特性。版本标签 `v0.1.2`，构建标签 `build-0.1.2-final-2`。
- GitHub Actions run `31304908863` 三端全部成功：`npm run check`、Windows 8 页面零付费调用点击烟测、Windows 中文项目创建 Rust 测试，以及三端 Tauri 原生构建/上传均通过。运行页：`https://github.com/zhou9527-lj/listingforge/actions/runs/31304908863`。
- Actions artifact：Windows x64 ID `9035782002`，`6,654,674` bytes，digest `sha256:10e998483bd0deda3adcee9bb312be940f6b9555436de150063e8b7ad21ebe67`；macOS Intel ID `9035791363`，`18,906,191` bytes，digest `sha256:4e5d6d92cb0408a6d070f99778f1641ad8fdfa7dc1bb8b599fc03a1b04ece656`；macOS Apple Silicon ID `9035666493`，`17,847,435` bytes，digest `sha256:090a7f3bbc848194fe05c952a638d27dae6020c284cce68b985026de1722ef40`。

### 0.1.2 正式安装包下载

- 发布页：`https://github.com/zhou9527-lj/listingforge/releases/tag/v0.1.2`。
- Windows x64：`https://github.com/zhou9527-lj/listingforge/releases/download/v0.1.2/ListingForge_0.1.2_windows-x64-setup.exe`，`6,734,166` bytes，SHA-256 `7f5fb26405f56b3636f96582f8e1418a572199976ff2511c56ea8717b6f7f81a`。
- macOS Intel：`https://github.com/zhou9527-lj/listingforge/releases/download/v0.1.2/ListingForge_0.1.2_macos-intel.dmg`，`9,382,261` bytes，SHA-256 `8ee92a8b04e9643ec5774c1728ceda953f72cbaa01003eae2cc98e33297323bc`。
- macOS Apple Silicon：`https://github.com/zhou9527-lj/listingforge/releases/download/v0.1.2/ListingForge_0.1.2_macos-apple-silicon.dmg`，`8,789,414` bytes，SHA-256 `211c3e2318501c619f56fc352585be05643c830e961221877ba82ecbf032dbab`。
- 三个公开链接均已在未附带 GitHub API 凭据的下载请求中验证为 HTTP 200，下载字节数与 Release 记录一致；Release 资产不会随 Actions artifact 的 90 天保留期过期。
- 当前免费构建没有付费 Windows 商业代码签名或 Apple 公证；Windows SmartScreen / macOS Gatekeeper 可能提示风险，macOS 可右键应用并选择“打开”。

### 0.1.3 SQL ACL 修复包

- 用户在 `0.1.2` 创建项目时收到 `Command plugin:sql|execute not allowed by ACL`。根因是 capability 仅配置 `sql:default`；该默认权限只包含数据库 load/close/select，不包含 execute。
- `src-tauri/capabilities/default.json` 已加入 `sql:allow-execute`，允许应用执行 PRAGMA、INSERT、UPDATE、DELETE 等本地数据库写操作。
- 新增 `scripts/check-capabilities.mjs` 与 `npm run test:capabilities`，并纳入 `npm run check`；以后缺少 `sql:default` 或 `sql:allow-execute` 会直接使检查失败，避免模拟点击测试掩盖安装包 ACL。
- 版本已升级为 `0.1.3`。本地 capability 检查、46 项单测、TypeScript、Vite production build 与 8 页面零付费点击烟测均通过。
- 修复提交 `32d6a7a`，构建标签 `build-0.1.3-sql-acl`。GitHub Actions run `31306489288` 三端全部成功；Windows runner 的 capability 检查、8 页面点击烟测、中文项目创建 Rust 测试、NSIS 构建和 artifact 上传均为 success。运行页：`https://github.com/zhou9527-lj/listingforge/actions/runs/31306489288`。
- Actions artifact：Windows x64 ID `9036209017`，`6,652,519` bytes，digest `sha256:27fd8f2ee3556e2df92421b2ba37270ee0fc66862b2772cc62532fbcf78b409b`；macOS Intel ID `9036246712`，`18,908,344` bytes，digest `sha256:d183a853c7572444a69d2fdc28e387665421835e51e0d0586a459b9452d65331`；macOS Apple Silicon ID `9036136298`，`17,847,756` bytes，digest `sha256:be42ce8f824695a1dec99adc240328ffe138b2833ca3ecc79b21daa55e4f660e`。

### 0.1.3 正式安装包下载

- 发布页：`https://github.com/zhou9527-lj/listingforge/releases/tag/v0.1.3`。
- Windows x64：`https://github.com/zhou9527-lj/listingforge/releases/download/v0.1.3/ListingForge_0.1.3_windows-x64-setup.exe`，`6,732,011` bytes，SHA-256 `e4d4d667dfa0882479bbda3079f24d2f2e36c891e0b99a9c5104765745ed0bcd`。
- macOS Intel：`https://github.com/zhou9527-lj/listingforge/releases/download/v0.1.3/ListingForge_0.1.3_macos-intel.dmg`，`9,384,051` bytes，SHA-256 `6246d277939b96959729b49e7604880f3aacb61a7f88debea019392b1fd497d9`。
- macOS Apple Silicon：`https://github.com/zhou9527-lj/listingforge/releases/download/v0.1.3/ListingForge_0.1.3_macos-apple-silicon.dmg`，`8,789,692` bytes，SHA-256 `c125737f7604aab0f0e259b833da12daf982982e072ee07867b7850900dea316`。
- 三条公开下载均已在不附带 GitHub API 凭据的请求中验证为 HTTP 200；`0.1.2` 存在 SQL execute ACL 缺失，用户应安装 `0.1.3`，不要继续使用旧包验证项目创建。

### 0.1.4 画布编辑正式版

- 画布编辑器三能力：打开生成结果继续编辑（背景）、AI 局部精修（蒙版 + 指令，未画蒙版时自动激活笔刷并 3 秒闪烁引导）、自由合成排版（多图片 + 文本图层，置顶/置底、显隐、撤销重做、文档自动保存恢复）；素材库项目素材卡片新增"加入画布"。
- 结果图片与任务均支持删除：记录 + 文件同步删除，Rust 命令 canonicalize + `starts_with` 校验仅允许删除当前项目 `results/` 内文件；任务删除时 `results` 由外键级联清理。
- 生成页主图/参考图持久化恢复；修复结果页"4 张唯一图片显示成多行重复"（v5 迁移：`idx_results_task` + `saveDownloadedResult` 幂等 + `loadPersistedResults` 去重）。
- 导出成品（PNG/JPG/WebP/长图）临时隐藏蒙版对象，蒙版标注不会出现在成品图；PSD 保留蒙版标注层作为编辑参考。
- 版本已升级为 `0.1.4`。本地能力检查、47 项单测、TypeScript、Vite production build、8 页面零付费点击烟测（含画布空态/导入/图层/蒙版引导流程）均通过。
- 功能提交 `639b036`（画布/删除/持久化）+ 文档提交 `1b18f2c`，构建标签 `build-0.1.4-features`。GitHub Actions run `31320890507` 三端全部成功：Windows runner 的检查与烟测、中文项目创建 Rust 测试、NSIS 构建和 artifact 上传均 success。运行页：`https://github.com/zhou9527-lj/listingforge/actions/runs/31320890507`。
- Actions artifact：Windows x64 ID `9040298150`，`6,668,975` bytes，digest `sha256:42dc5e8a8bfe36eb20322ec24e218fb47d5a429b29a4e1bcd9be00e05964bba8`；macOS Intel ID `9040321370`，`18,947,686` bytes，digest `sha256:1890f6b2258270ecf35084dc328af520ded6818cb748bd37cb23c7b2d22e3d53`；macOS Apple Silicon ID `9040169032`，`17,858,545` bytes，digest `sha256:c9112c9142d4761a756cc8bcba474d76e6e86ac15381883bbc812b28d3a204b6`。
- 本地 NSIS 构建亦成功（`.toolchains` 工具链 + llvm-mingw lld + winlibs runtime libs），产物 10,606,383 bytes；CI 产物 6,748,460 bytes，差异来自构建环境与压缩，两者均可安装。

### 0.1.4 正式安装包下载

- 发布页：`https://github.com/zhou9527-lj/listingforge/releases/tag/v0.1.4`。
- Windows x64：`https://github.com/zhou9527-lj/listingforge/releases/download/v0.1.4/ListingForge_0.1.4_windows-x64-setup.exe`，`6,748,460` bytes，SHA-256 `fd99a4f0d2c5bfa93d44520397374293fda9b08471a1f69cbf063d84d698f03c`。
- macOS Intel：`https://github.com/zhou9527-lj/listingforge/releases/download/v0.1.4/ListingForge_0.1.4_macos-intel.dmg`，`9,404,718` bytes，SHA-256 `fed103611a142c7d7168b1589cfdac41e5e31f05d85a1bcae65c36ed6716345e`。
- macOS Apple Silicon：`https://github.com/zhou9527-lj/listingforge/releases/download/v0.1.4/ListingForge_0.1.4_macos-apple-silicon.dmg`，`8,794,492` bytes，SHA-256 `7783755394e51022be34f3cadcd60662b68558a487b1809a5147574141b16136`。
- 三条公开下载均已在不附带 GitHub API 凭据的请求中验证为 HTTP 200（含重定向跟随），下载字节数与 Release 记录一致；Release 资产不会随 Actions artifact 的 90 天保留期过期。
- 当前免费构建没有付费 Windows 商业代码签名或 Apple 公证；Windows SmartScreen / macOS Gatekeeper 可能提示风险，macOS 可右键应用并选择"打开"。

### 0.1.5 任务中心排版修复版

- 修复任务列表行被裁切：任务表格此前被压在固定 54px 高的残留网格行里，任意窗口大小下都只能看到第一行顶部一条；现已释放回剩余高度（`1fr`），任务行完整显示，分页栏固定底部。
- 窄窗口自动隐藏次要列：窗口 ≤1435px 时隐藏「项目」「用时」两列，核心信息（任务 / 提供方 / 进度 / 消耗 / 操作）始终完整可见，无需横向滚动；空任务队列时引导卡片撑满工作区。
- 布局根因通过无头浏览器多视口实测定位（非猜测）：`.task-workspace` 5 行网格模板 vs JSX 4 个 child，table 落入固定 54px 行；修复为 4 行模板 + 表格条件渲染，修复后所有视口 table 高度随 `1fr` 变化、无横向滚动。
- 版本已升级为 `0.1.5`。本地能力检查、47 项单测、TypeScript、Vite production build 均通过；`npm run check` 全绿。
- 修复提交 `bf780a2`，构建标签 `build-0.1.5-tasks-layout`。GitHub Actions run `31326806604` 三端全部成功（Windows NSIS、macOS Apple Silicon、macOS Intel）。运行页：`https://github.com/zhou9527-lj/listingforge/actions/runs/31326806604`。
- Actions artifact：Windows x64 ID `9041919055`，`6,671,512` bytes，digest `sha256:d9095859e41f948c8fd3919703a05fec5200ef36403f7ec9a86320fa565a55e5`；macOS Intel ID `9041906949`，`18,945,985` bytes，digest `sha256:b71eb206de4ceed6c328faa2380465e4de576e11388cf262564f6a495ee26c05`；macOS Apple Silicon ID `9041789992`，`17,857,545` bytes，digest `sha256:520ac0161022edf42a2ad8792cc2ce9f8295939654d96fe8832cdf705a601e4d`。

### 0.1.5 正式安装包下载

- 发布页：`https://github.com/zhou9527-lj/listingforge/releases/tag/v0.1.5`。
- Windows x64：`https://github.com/zhou9527-lj/listingforge/releases/download/v0.1.5/ListingForge_0.1.5_windows-x64-setup.exe`，`6,750,996` bytes，SHA-256 `47dc35084c36762be89547e2a87c524b77f3d221a56666e1a166d88181a85b47`。
- macOS Intel：`https://github.com/zhou9527-lj/listingforge/releases/download/v0.1.5/ListingForge_0.1.5_macos-intel.dmg`，`9,402,836` bytes，SHA-256 `6ca0fb4521bb2169ad60bfa327359ba07a5629e7f77d0f31bfc537e8ea3ff180`。
- macOS Apple Silicon：`https://github.com/zhou9527-lj/listingforge/releases/download/v0.1.5/ListingForge_0.1.5_macos-apple-silicon.dmg`，`8,794,117` bytes，SHA-256 `dd84f19f29960b7be01ab3cc758bfa768e05ebf669902804d4b80f0b2e8f0a7b`。
- 三条公开下载均已在不附带 GitHub API 凭据的请求中验证为 HTTP 200（含重定向跟随），下载字节数与 Release 记录一致；Release 资产不会随 Actions artifact 的 90 天保留期过期。

### 0.1.6 结果页瀑布流版

- 结果页图片展示重构为瀑布流画廊：按图片原始比例排布（方图矮、长图高），宽窗口 3 列、窄窗口 2 列，窗口缩放自动重排，一次看到 9-12 张。
- 长图友好：细节长图（1242×2688）卡片限高展示顶部、悬停提示「查看全图」；点击任意图片弹出结果灯箱——完整大图预览，滚轮缩放 1–5x、缩放后拖动平移、双击复位、左右翻页（←/→ 快捷键）、Esc 关闭，收藏 / 删除 / 编辑（打开画布）全部在灯箱内完成。
- 悬停控制层：卡片默认纯净无遮挡，悬停浮现选择 / 收藏 / 删除按钮与标题、尺寸、生成时间；已选中卡片控制层常驻；保留「瀑布流 / 列表」双视图切换。
- 布局实现要点（无头浏览器多视口实测驱动）：callback ref + React 19 ref cleanup 挂载 ResizeObserver（规避 effect 依赖顺序导致的 TDZ）；列宽剥离容器水平 padding；列数阈值以窗口宽度对齐 ≤1359px 布局断点；`LightboxStage` 以 `key={item.id}` 重挂载重置缩放/平移状态。
- 版本已升级为 `0.1.6`。本地能力检查、47 项单测、TypeScript、Vite production build 均通过；`npm run check` 全绿。
- 功能提交 `4a0a7c8`（瀑布流画廊 + 灯箱）、`59a9afa`（docs 交接记录）；首次构建 run `31355379171` 因 click-test 任务删除断言时序 flaky 失败（文件删除经额外 IPC 往返，`waitForFunction` 只覆盖记录删除），已加固断言（`7e00de5`：等待 `deletedFiles` 最多 5s 再断言，结果页 / 任务页两处同改），本地复跑通过。
- 修复提交 `7e00de5`，构建标签 `build-0.1.6-results-gallery`。GitHub Actions run `31356419227` 三端全部成功（Windows NSIS、macOS Apple Silicon、macOS Intel）。运行页：`https://github.com/zhou9527-lj/listingforge/actions/runs/31356419227`。
- Actions artifact：Windows x64 ID `9050994605`，`6,673,505` bytes，digest `sha256:13709970d8db8ce65aafdffa4149ed3b3f64368a601d6d46ce7f2e88aa9a764e`；macOS Intel ID `9050910486`，`18,948,620` bytes，digest `sha256:216b900303c9e2b01acfb2affa2825f5e6f9156f342074c3351fec38baec9bf8`；macOS Apple Silicon ID `9050853008`，`17,861,713` bytes，digest `sha256:8846fbcf451330270369c561d0afa9fc85d48171c80743b99eec654820af0b96`。

### 0.1.6 正式安装包下载

- 发布页：`https://github.com/zhou9527-lj/listingforge/releases/tag/v0.1.6`。
- Windows x64：`https://github.com/zhou9527-lj/listingforge/releases/download/v0.1.6/ListingForge_0.1.6_windows-x64-setup.exe`，`6,752,991` bytes，SHA-256 `5641dab053080ae65faeba5e3905bf9c0400a2580841c072de948e950416fb21`。
- macOS Intel：`https://github.com/zhou9527-lj/listingforge/releases/download/v0.1.6/ListingForge_0.1.6_macos-intel.dmg`，`9,405,443` bytes，SHA-256 `150cee3dc56ea731c76073ab1e1a1f531cc5c2eaebb4ce9c5246f19eb4bec10a`。
- macOS Apple Silicon：`https://github.com/zhou9527-lj/listingforge/releases/download/v0.1.6/ListingForge_0.1.6_macos-apple-silicon.dmg`，`8,797,522` bytes，SHA-256 `0033bf964706ef5b306e9df74cdfef48f03e15b75702a8bb8c5c139e35419d4d`。
- 三条公开下载均已在不附带 GitHub API 凭据的请求中验证为 HTTP 200（含重定向跟随），下载字节数与 Release 记录一致；Release 资产不会随 Actions artifact 的 90 天保留期过期。

## 发布检查

版本号、变更日志、许可证、第三方声明、模型许可证、SBOM、测试报告、SHA-256、安装/卸载、离线启动、密钥迁移和工程兼容性均通过后才发布。

## 自动更新

不加入自动更新功能。用户从 GitHub Releases 手动下载新版本，应用只在“关于”页显示当前版本，不主动检查更新。
